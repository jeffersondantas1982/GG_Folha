<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GG | Gestão de Gente</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Outfit', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#f0fdfa',
                            100: '#ccfbf1',
                            200: '#99f6e4',
                            300: '#5eead4',
                            400: '#2dd4bf',
                            500: '#14b8a6',
                            600: '#0d9488',
                            700: '#0f766e',
                            800: '#115e59',
                            900: '#134e4a',
                        },
                        accent: {
                            500: '#a855f7',
                            600: '#9333ea',
                            700: '#7e22ce',
                        }
                    }
                }
            }
        }
    </script>
    <style type="text/tailwindcss">
        @layer utilities {
            .glass {
                @apply bg-white bg-opacity-80 backdrop-blur-md border border-white border-opacity-20;
            }
            .btn-primary {
                @apply bg-gradient-to-r from-brand-500 to-accent-500 text-white font-semibold py-2.5 px-6 rounded-xl shadow-lg shadow-brand-200 hover:shadow-xl hover:scale-[1.02] transition-all active:scale-95;
            }
            .btn-secondary {
                @apply bg-white text-brand-700 font-semibold py-2.5 px-6 rounded-xl border border-brand-100 hover:bg-brand-50 transition-all;
            }
            .input-field {
                @apply w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-brand-400 focus:border-transparent transition-all;
            }
            .card {
                @apply bg-white rounded-2xl shadow-sm border border-slate-100 p-6;
            }
            .logo-gg {
                background: linear-gradient(135deg, #14b8a6 0%, #a855f7 100%);
            }
        }
        
        body {
            background: radial-gradient(circle at top right, #f0fdfa, #faf5ff);
            min-height: 100vh;
        }
    </style>
</head>

<body class="text-slate-900 overflow-x-hidden">

    <nav class="sticky top-0 z-50 glass border-b border-brand-100 mb-8">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <div class="flex items-center space-x-4">
                <a href="/" class="flex items-center space-x-3 group">
                    <div
                        class="bg-white p-1 rounded-lg shadow-sm border border-brand-50 group-hover:shadow transition-all shrink-0">
                        <img src="/img/system_logo.png" alt="Folha GG" class="h-9 w-auto object-contain">
                    </div>
                    <% if (institution.logotipo) { %>
                        <div class="h-8 w-px bg-slate-200 hidden md:block"></div>
                        <img src="<%= institution.logotipo %>" alt="Unidade Logo"
                            class="h-8 w-auto object-contain hidden md:block" onerror="this.style.display='none'">
                        <% } %>
                </a>
                <div class="h-8 w-px bg-slate-200 hidden md:block"></div>
                <div class="hidden md:block">
                    <div class="text-[10px] font-bold text-slate-400 uppercase tracking-widest leading-none">Unidade
                    </div>
                    <div class="text-sm font-bold text-slate-700 uppercase">
                        <%= institutionName %>
                    </div>
                </div>
            </div>
            <div class="flex items-center space-x-6">
                <a href="/" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Equipe</a>
                <a href="/timesheet"
                    class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Folhas</a>
                <a href="/users" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Usuários</a>
                <a href="/settings"
                    class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Configurações</a>
                <a href="/backup/download"
                    class="flex items-center gap-1.5 text-slate-600 hover:text-brand-600 font-medium transition-colors"
                    title="Baixar cópia do banco de dados (Excel)">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M9 19l3 3m0 0l3-3m-3 3V10">
                        </path>
                    </svg>
                    <span>Backup</span>
                </a>
                <a href="/help" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Ajuda</a>
                <a href="/about" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Sobre</a>
                <a href="/logout"
                    class="p-2 text-slate-400 hover:text-red-600 hover:bg-red-50 rounded-lg transition-all"
                    title="Sair">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1">
                        </path>
                    </svg>
                </a>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-6 pb-20 animate-in fade-in slide-in-from-bottom-4 duration-700">