<%- include('header') %>

    <div class="mb-10 flex flex-col md:flex-row md:items-end justify-between gap-4">
        <div>
            <h1 class="text-3xl font-bold text-slate-800 uppercase">Colaboradores Inativos</h1>
            <p class="text-slate-500 italic">Lista de profissionais desligados ou com contrato suspenso.</p>
        </div>
        <a href="/" class="btn-secondary inline-flex items-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18">
                </path>
            </svg>
            Voltar ao Painel
        </a>
    </div>

    <div class="card mb-8">
        <form action="/inactives" method="GET" class="p-2 flex gap-4">
            <div class="relative flex-1">
                <input type="text" name="search" value="<%= searchQuery %>"
                    placeholder="Pesquisar por nome ou matrícula..."
                    class="w-full pl-10 pr-4 py-3 bg-slate-50 border border-slate-200 rounded-2xl focus:ring-2 focus:ring-brand-400 outline-none transition-all">
                <div class="absolute left-3 top-3.5 text-slate-400">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                    </svg>
                </div>
            </div>
            <button type="submit" class="btn-primary px-8">Filtrar</button>
        </form>
    </div>

    <div class="card overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full text-left">
                <thead>
                    <tr class="bg-slate-50 text-slate-500 text-xs font-bold uppercase tracking-wider">
                        <th class="px-6 py-4">Colaborador</th>
                        <th class="px-6 py-4">Identificação</th>
                        <th class="px-6 py-4">Cargo & Unidade</th>
                        <th class="px-6 py-4 text-center">Ações</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100">
                    <% if (professionals.length===0) { %>
                        <tr>
                            <td colspan="4" class="py-20 text-center">
                                <div class="text-4xl mb-4">📂</div>
                                <p class="text-slate-400 italic font-medium">Nenhum registro inativo encontrado.</p>
                            </td>
                        </tr>
                        <% } else { %>
                            <% professionals.forEach(p=> { %>
                                <tr class="hover:bg-slate-50/50 transition-colors">
                                    <td class="px-6 py-4">
                                        <span class="font-bold text-slate-900 uppercase block">
                                            <%= p.nome %>
                                        </span>
                                        <span class="text-[10px] text-slate-400 uppercase font-black">SITUAÇÃO:
                                            INATIVO</span>
                                    </td>
                                    <td class="px-6 py-4">
                                        <div class="text-xs font-bold text-slate-400 uppercase">Matrícula</div>
                                        <div class="text-sm font-mono text-slate-600">
                                            <%= p.matricula %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4">
                                        <div class="text-sm font-bold text-slate-700 uppercase">
                                            <%= p.cargo %>
                                        </div>
                                        <div class="text-xs text-slate-500 uppercase">
                                            <%= p.unidade_lotacao %>
                                        </div>
                                    </td>
                                    <td class="px-6 py-4">
                                        <div class="flex items-center justify-center">
                                            <form action="/status/update/<%= p.matricula %>" method="POST">
                                                <input type="hidden" name="status" value="ATIVO">
                                                <button type="submit"
                                                    class="bg-green-50 text-green-600 hover:bg-green-600 hover:text-white px-4 py-2 rounded-xl text-xs font-black uppercase tracking-widest transition-all ring-1 ring-green-200">
                                                    Reativar
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                                <% }) %>
                                    <% } %>
                </tbody>
            </table>
        </div>
    </div>

    <%- include('footer') %>