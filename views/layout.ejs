<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HPV | Sistema de Folha de Ponto</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Outfit', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#f0f7ff',
                            100: '#e0effe',
                            200: '#bae0fd',
                            300: '#7cc7fb',
                            400: '#38a9f8',
                            500: '#0e8ce9',
                            600: '#026ec7',
                            700: '#0358a1',
                            800: '#074a85',
                            900: '#0c3f6e',
                        },
                    }
                }
            }
        }
    </script>
    <style type="text/tailwindcss">
        @layer utilities {
            .glass {
                @apply bg-white bg-opacity-80 backdrop-blur-md border border-white border-opacity-20;
            }
            .btn-primary {
                @apply bg-gradient-to-r from-brand-600 to-brand-500 text-white font-semibold py-2.5 px-6 rounded-xl shadow-lg shadow-brand-200 hover:shadow-xl hover:scale-[1.02] transition-all active:scale-95;
            }
            .btn-secondary {
                @apply bg-white text-brand-700 font-semibold py-2.5 px-6 rounded-xl border border-brand-100 hover:bg-brand-50 transition-all;
            }
            .input-group {
                @apply relative transition-all;
            }
            .input-field {
                @apply w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-3 focus:outline-none focus:ring-2 focus:ring-brand-400 focus:border-transparent transition-all;
            }
            .card {
                @apply bg-white rounded-2xl shadow-sm border border-slate-100 p-6;
            }
        }
        
        body {
            background: radial-gradient(circle at top right, #f8fafc, #eff6ff);
            min-height: 100vh;
        }
    </style>
</head>

<body class="text-slate-900 overflow-x-hidden">

    <nav class="sticky top-0 z-50 glass border-b border-slate-200 mb-8">
        <div class="container mx-auto px-6 py-4 flex justify-between items-center">
            <a href="/" class="flex items-center space-x-2">
                <div
                    class="w-10 h-10 bg-brand-600 rounded-lg flex items-center justify-center text-white font-bold shadow-lg">
                    IA</div>
                <span
                    class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-brand-800 to-brand-600">HPV
                    Folha</span>
            </a>
            <div class="flex items-center space-x-8">
                <a href="/" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Profissionais</a>
                <a href="/timesheet" class="text-slate-600 hover:text-brand-600 font-medium transition-colors">Gerar
                    Folhas</a>
                <a href="/register" class="btn-primary">Novo Cadastro</a>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-6 pb-20 animate-in fade-in slide-in-from-bottom-4 duration-700">
        <%- body %>
    </main>

    <footer class="text-center py-8 text-slate-400 text-sm border-t border-slate-100 bg-white">
        &copy; 2026 <%= institutionName %> - Sistema de Gestão de Ponto Profissional
    </footer>

</body>

</html>