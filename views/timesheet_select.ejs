<%- include('header') %>

    <div class="max-w-xl mx-auto">
        <div class="mb-8 text-center">
            <h1 class="text-3xl font-bold text-slate-800">Folhas de Ponto</h1>
            <p class="text-slate-500">Selecione o período para gerar os documentos de impressão.</p>
        </div>

        <div class="card p-8 bg-gradient-to-br from-white to-slate-50">
            <form action="/timesheet/print" method="GET" target="_blank" class="space-y-6">

                <div class="space-y-2">
                    <label class="block text-sm font-bold text-slate-700">Mês de Referência</label>
                    <select name="month" class="input-field appearance-none cursor-pointer">
                        <% ["Janeiro", "Fevereiro" , "Março" , "Abril" , "Maio" , "Junho" , "Julho" , "Agosto"
                            , "Setembro" , "Outubro" , "Novembro" , "Dezembro" ].forEach((m, i)=> { %>
                            <option value="<%= i %>" <%=(new Date().getMonth()===i) ? 'selected' : '' %>><%= m %>
                            </option>
                            <% }) %>
                    </select>
                </div>

                <div class="space-y-2">
                    <label class="block text-sm font-bold text-slate-700">Ano</label>
                    <input type="number" name="year" class="input-field" value="<%= new Date().getFullYear() %>">
                </div>

                <div class="space-y-2">
                    <label class="block text-sm font-bold text-slate-700">Filtrar por Unidade (Opcional)</label>
                    <select name="unidade" class="input-field appearance-none cursor-pointer">
                        <option value="">TODAS AS UNIDADES</option>
                        <% units.sort().forEach(unit=> { %>
                            <option value="<%= unit %>">
                                <%= unit %>
                            </option>
                            <% }) %>
                    </select>
                </div>

                <div class="pt-4">
                    <button type="submit" class="btn-primary w-full flex items-center justify-center space-x-2">
                        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m2 4h6a2 2 0 002-2v-4a2 2 0 00-2-2H9a2 2 0 00-2 2v4a2 2 0 002 2zm8-12V5a2 2 0 00-2-2H9a2 2 0 00-2 2v4h10z">
                            </path>
                        </svg>
                        <span>Gerar e Abrir Impressão</span>
                    </button>
                    <p class="text-xs text-center text-slate-400 mt-4">
                        O sistema irá gerar uma página individual para cada profissional cadastrado.
                    </p>
                </div>

            </form>
        </div>
    </div>

    <%- include('footer') %>